const publicRoutes = {
  index: '/',
  login: '/login',
};

const privateRoutes = {
  home: '/',
  staff: '/staff',
  parts: '/parts',
  jobs: '/jobs',
  payments: '/payments',
  customers: '/customers',
};

export default { publicRoutes, privateRoutes };
