const publicRoutes = {
  login: '/login',
  index: '/',
};

const privateRoutes = {
  home: '/home',
  staff: '/staff',
  parts: '/parts',
  jobs: '/jobs',
  payments: '/payments',
};

export default { publicRoutes, privateRoutes };
