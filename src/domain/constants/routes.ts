const publicRoutes = {
  login: '/login',
  index: '/',
};

const privateRoutes = {
  home: '/home',
  staff: '/staff',
  parts: '/parts',
  jobs: '/jobs',
  payments: '/payments',
  customers: '/customers',
};

export default { publicRoutes, privateRoutes };
