const publicRoutes = {
  index: '/',
  login: '/login',
};

const privateRoutes = {
  home: '/',
  users: '/users',
  parts: '/parts',
  jobs: '/jobs',
  payments: '/payments',
  customers: '/customers',
};

export default { publicRoutes, privateRoutes };
